diff --git a/esm/src/transformers/storyblok.js b/esm/src/transformers/storyblok.js
index 1234567..abcdefg 100644
--- a/esm/src/transformers/storyblok.js
+++ b/esm/src/transformers/storyblok.js
@@ -4,11 +4,12 @@ export const splitFilters = (filters) => {
     if (!filters) {
         return {};
     }
-    return Object.fromEntries(filters.split(":").map((filter) => {
-        if (!filter)
-            return [];
-        const [key, value] = filter.split("(");
-        return [key, value.replace(")", "")];
-    }));
+    const result = {};
+    const regex = /(\w+)\(([^)]*)\)/g;
+    let match;
+    while ((match = regex.exec(filters)) !== null) {
+        result[match[1]] = match[2];
+    }
+    return result;
 };
 export const generateFilters = (filters) => {
diff --git a/script/src/transformers/storyblok.js b/script/src/transformers/storyblok.js
index 1234567..abcdefg 100644
--- a/script/src/transformers/storyblok.js
+++ b/script/src/transformers/storyblok.js
@@ -7,11 +7,12 @@ const splitFilters = (filters) => {
     if (!filters) {
         return {};
     }
-    return Object.fromEntries(filters.split(":").map((filter) => {
-        if (!filter)
-            return [];
-        const [key, value] = filter.split("(");
-        return [key, value.replace(")", "")];
-    }));
+    const result = {};
+    const regex = /(\w+)\(([^)]*)\)/g;
+    let match;
+    while ((match = regex.exec(filters)) !== null) {
+        result[match[1]] = match[2];
+    }
+    return result;
 };
 exports.splitFilters = splitFilters;
