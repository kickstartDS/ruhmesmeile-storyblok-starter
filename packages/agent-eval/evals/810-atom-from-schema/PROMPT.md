Implement the `Badge` component for this kickstartDS design system package.

Its props are already specified in `src/components/badge/badge.schema.json` — treat that schema as the source of truth for the component's API.

Requirements:

- Implement the component under `src/components/badge/`.
- Follow this design system's conventions for authoring a component: file layout, naming, styling approach, and how interactive behaviour is implemented.
- Support every property defined in the schema.
- A dismissible badge must actually be dismissible at runtime.
- Styling must use the design system's tokens rather than literal values.

Do not modify `badge.schema.json`.
