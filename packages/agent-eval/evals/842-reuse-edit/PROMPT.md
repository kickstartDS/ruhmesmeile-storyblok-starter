# Task

`PromoTile` predates `Button` and hand-rolls its own — markup, styles, icon
span and all. Now that we have the real one, please swap it over.

Its props stay exactly as they are; this shouldn't be visible to anyone calling
it. The headline and the copy are fine as they stand — it's only the action
that needs moving.
