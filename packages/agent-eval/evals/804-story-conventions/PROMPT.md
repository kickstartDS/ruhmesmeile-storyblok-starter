# Task

`PriceTag` is finished and in use, but it never got documented — it's the only
component in the package without a Storybook entry, so it doesn't show up in the
component library at all.

Please write its Storybook documentation, following the conventions the rest of
this design system's components use for theirs.

The component itself is done; this is a documentation job.
