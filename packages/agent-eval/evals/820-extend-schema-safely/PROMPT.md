# Task

The comment list needs a smaller `Avatar` than the one we have — same
component, less of it. Please give it a `size` option with a compact variant
alongside what it does today.

`Avatar` is already published and used in a few places, so whatever we add has
to leave those call sites alone: nobody should have to go and edit an existing
usage because of this.
